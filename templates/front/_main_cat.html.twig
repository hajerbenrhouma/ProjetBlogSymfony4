
  {%  for  category in categories %}
      <a class="p-2 text-dark" href="{{ path('videoListe',
          {'categoryname': category.name|lower|slugify ,
          'id': category.id}) }}">{{ category.name }}</a>
{% endfor %}